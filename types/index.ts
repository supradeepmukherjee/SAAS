export interface Video {
    id: string
    title: string
    desc: string
    publicID: string
    origSize: string
    compressedSize: string
    duration: number
    createdAt: Date
    updatedAt: Date
}